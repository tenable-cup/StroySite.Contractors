<Page x:Class="StroySite.WPF.AddEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="400"
      Title="Редактирование контрагента"
      Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="{Binding Title}" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Наименование -->
        <Label Grid.Row="1" Grid.Column="0" Content="Наименование:" Margin="5"/>
        <TextBox x:Name="txtFullName" Grid.Row="1" Grid.Column="1" Margin="5,6,5,0" Height="NaN" MaxLength="200"/>

        <!-- Телефон -->
        <Label Grid.Row="2" Grid.Column="0" Content="Телефон:" Margin="5"/>
        <TextBox x:Name="txtPhone" Grid.Row="2" Grid.Column="1" Margin="5,7,5,38" Height="NaN" MaxLength="20" Grid.RowSpan="2"/>

        <!-- Email -->
        <Label Grid.Row="3" Grid.Column="0" Content="Email:" Margin="5"/>
        <TextBox x:Name="txtEmail" Grid.Row="3" Grid.Column="1" Margin="5,7,5,0" Height="NaN" MaxLength="100"/>

        <!-- Адрес -->
        <Label Grid.Row="4" Grid.Column="0" Content="Адрес:" Margin="5"/>
        <TextBox x:Name="txtAddress" Grid.Row="4" Grid.Column="1" Margin="5,18,5,5" Height="NaN" MaxLength="250"
                 TextWrapping="Wrap" AcceptsReturn="True"/>

        <!-- Тип (выпадающий список) -->
        <Label Grid.Row="5" Grid.Column="0" Content="Тип:" Margin="5"/>
        <ComboBox x:Name="cmbType" Grid.Row="5" Grid.Column="1" Margin="5" Height="25"
                  DisplayMemberPath="TypeName" SelectedValuePath="Id"/>

        <!-- Кнопки -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Left" Margin="46,-13,0,19" Grid.RowSpan="2">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Height="30" Margin="10" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="30" Margin="10" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Page>